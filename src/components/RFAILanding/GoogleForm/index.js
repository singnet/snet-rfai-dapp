import React from "react";

import { withStyles } from "@material-ui/styles";

import { useStyles } from "./styles";

const GoogleForm = ({ classes }) => {
  return <span>google form</span>;
};

export default withStyles(useStyles)(GoogleForm);
